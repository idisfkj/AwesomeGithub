<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.idisfkj.awesome.user">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:name=".UserApp"
            android:allowBackup="true"
            android:icon="@mipmap/user_ic_launcher"
            android:label="@string/user_app_name"
            android:roundIcon="@mipmap/user_ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/UserAppTheme">
        <activity android:name="com.idisfkj.awesome.user.UserMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>