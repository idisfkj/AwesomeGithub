<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.idisfkj.awesome.search">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:name=".SearchApp"
            android:allowBackup="true"
            android:icon="@mipmap/search_ic_launcher"
            android:label="@string/search_app_name"
            android:roundIcon="@mipmap/search_ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/SearchAppTheme">
        <activity android:name="com.idisfkj.awesome.search.SearchMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>